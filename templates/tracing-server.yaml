apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-tracing-server
  namespace: {{ .Values.namespace }}
  labels:
    app: tracing-server
    app.kubernetes.io/name: tracing-server
    app.kubernetes.io/instance: {{ .Release.Name }}-tracing-server
spec:
  replicas: {{ .Values.tracing_server.replicaCount }}
  selector:
    matchLabels:
      app: tracing-server
  template:
    metadata:
      labels:
        app: tracing-server
    spec:
      containers:
        - name: tracing-server
          image: "{{ .Values.tracing_server.image }}:{{ .Values.tracing_server.tag }}"
          ports:
            - containerPort: {{ .Values.tracing_server.port }}
          resources:
            {{- toYaml .Values.tracing_server.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.tracing_server.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tracing-server-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: tracing-server
spec:
  selector:
    app: tracing-server
  ports:
    - protocol: TCP
      port: {{ .Values.tracing_server.port }}
      targetPort: {{ .Values.tracing_server.port }}
    {{- if .Values.tracing_server.nodePort }}
    nodePort: {{ .Values.tracing_server.nodePort }}
    {{- end }}
  type: {{ .Values.tracing_server.serviceType | default "LoadBalancer" }}