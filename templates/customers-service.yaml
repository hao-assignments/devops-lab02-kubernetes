apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-customers-service
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
spec:
  replicas: {{ .Values.customers_service.replicaCount }}
  selector:
    matchLabels:
      app: customers-service
  template:
    metadata:
      labels:
        app: customers-service
    spec:
      containers:
        - name: customers-service
          image: "{{ .Values.customers_service.image }}"
          ports:
            - containerPort: {{ .Values.customers_service.port }}
          resources:
            limits:
              memory: {{ .Values.customers_service.resources.limits.memory | quote }}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.customers_service.env.SPRING_PROFILES_ACTIVE | quote }}
            - name: SPRING_CLOUD_CONFIG_URI
              value: {{ .Values.customers_service.env.SPRING_CLOUD_CONFIG_URI | replace "config-server" (printf "%s-config-server-svc" .Release.Name) | quote }}
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: {{ .Values.customers_service.env.EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE | replace "discovery-server" (printf "%s-discovery-server-svc" .Release.Name) | quote }}
            - name: EUREKA_INSTANCE_HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: {{ .Values.customers_service.env.MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE | quote }}
            - name: SPRING_CLOUD_DISCOVERY_HEALTH_TIMEOUT
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_HEALTH_TIMEOUT | quote }}
            - name: SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_USE-DISCOVERY-METADATA
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_USE_DISCOVERY_METADATA | quote }}
            - name: SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_MANAGEMENT_PORT
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_MANAGEMENT_PORT | quote }}
            - name: SPRING_CLOUD_DISCOVERY_HEALTH_RETRY_MAX_ATTEMPTS
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_HEALTH_RETRY_MAX_ATTEMPTS | quote }}
            - name: SPRING_CLOUD_DISCOVERY_HEALTH_RETRY_INITIAL_INTERVAL
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_HEALTH_RETRY_INITIAL_INTERVAL | quote }}
            - name: SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_MANAGEMENT_BASE_URL
              value: {{ .Values.customers_service.env.SPRING_CLOUD_DISCOVERY_CLIENT_HEALTHINDICATOR_MANAGEMENT_BASE_URL | quote }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.customers_service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-customers-service-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
spec:
  selector:
    app: customers-service
  ports:
    - protocol: TCP
      port: {{ .Values.customers_service.port }}
      targetPort: {{ .Values.customers_service.port }}