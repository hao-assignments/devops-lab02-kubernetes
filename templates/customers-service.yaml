apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-customers-service
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
    app.kubernetes.io/name: customers-service
    app.kubernetes.io/instance: {{ .Release.Name }}-customers-service
spec:
  replicas: {{ .Values.customers_service.replicaCount }}
  selector:
    matchLabels:
      app: customers-service
  template:
    metadata:
      labels:
        app: customers-service
    spec:
      containers:
        - name: customers-service
          image: "{{ .Values.customers_service.image }}:{{ .Values.customers_service.tag }}"
          ports:
            - containerPort: {{ .Values.customers_service.port }}
          resources:
            {{- toYaml .Values.customers_service.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.customers_service.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.customers_service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-customers-service-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
spec:
  selector:
    app: customers-service
  ports:
    - protocol: TCP
      port: {{ .Values.customers_service.port }}
      targetPort: {{ .Values.customers_service.port }}